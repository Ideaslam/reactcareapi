 
const Sequelize = require('./db');
const User = require('./models/user-model');



//FeatureType.sync({force:true});
//Feature.sync({force:true});
 //User.sync({force:true});
 //PLace.sync({force:true});
//MasterCode.sync({force:true});
 //MasterCodeDetails.sync({force:true});
 //PLace.sync({force:true});
 //Nvr.sync({force:true});
 //Camera.sync({force:true});
  //Request.sync({force:true});


// chagne column 

// return queryInterface.changeColumn('requests', 'name', {
//   type: Sequelize.STRING(150),
// })


// Create table 

// return queryInterface.addColumn(
//   'Todo',
//   'completed',
//  Sequelize.BOOLEAN
// );


//forign Key 
// references: { model: 'users', key: 'id' }